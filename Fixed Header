/* ===============Fixed Header============== */
$(window).scroll(function(){
    if ($(window).scrollTop() >= 500) {
      $('header').addClass('fixed');
    }
    else {
      $('header').removeClass('fixed');
    }
});
